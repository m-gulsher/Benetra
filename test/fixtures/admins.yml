# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html
# Admin records are auto-created when users with role "admin" are created.
# Admin fixtures are not needed as they are created via the User model's after_create callback.
