<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin Dashboard</title>
  <%= tag.link rel: "stylesheet", href: "https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" %>
  <%= stylesheet_link_tag "tailwind", "application", "inter-font", "data-turbo-track": "reload" %>
  <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  <%= turbo_include_tags %>
  <script>
    function toggleSidebar() {
      const sidebar = document.getElementById("sidebar");
      sidebar.classList.toggle("hidden");
    }
    function closeFlashMessage(button) {
      const flashMessage = button.closest("#flash-message");
      flashMessage.remove();
      location.reload();
    }
  </script>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
</head>
<body class="bg-gray-50 h-screen overflow-hidden">
  <div class="flex flex-col h-full">
    <%= render "layouts/topbar" %>
    <div>
      <% flash.each do |type, message| %>
        <div class="<%= flash_class(type) %>" id="flash-message" style="position: relative;">
          <button class="absolute top-2 right-2 text-gray-600 hover:text-gray-800 focus:outline-none" onclick="closeFlashMessage(this)">
            <i class="bi bi-x"></i>
          </button>
          <%= message %>
        </div>
      <% end %>
    </div>
    <div class="flex flex-1">

      <% if user_signed_in? %>
        <aside id="sidebar" class="bg-white w-64 border-r border-gray-200 lg:block hidden">
          <%= render "layouts/sidebar" %>
        </aside>
      <% else %>
        <% if params[:controller] == "home" %>
          <div class="flex items-center justify-center h-full p-6">
            <div class="max-w-sm w-full bg-white border border-gray-200 rounded-lg shadow-md p-6">
              <h2 class="text-xl font-semibold mb-4">Welcome to Ichra</h2>
              <p class="text-gray-700 mb-4">Please sign up or sign in to continue accessing the Dashboard</p>
              <div class="flex justify-between">
                <%= link_to 'Sign Up', new_user_registration_path, class: "bg-blue-500 text-white px-4 py-2 rounded-md" %>
                <%= link_to 'Sign In', new_user_session_path, class: "bg-gray-500 text-white px-4 py-2 rounded-md" %>
              </div>
            </div>
          </div>
        <% end %>
      <% end %>

      <div class="flex-1 flex flex-col h-screen w-screen">
        <main class="flex-grow overflow-auto p-2 mt-2 lg:ml-4 lg:mr-4">
          <%= yield %>
        </main>
      </div>
    </div>
  </div>
</body>
</html>